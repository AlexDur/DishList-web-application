const styles = {
  header: {
    marginTop: 50,
    fontSize: 30,

    color: "blue",
    maxWidth: "100%",
  },
};

const Header = () => {
  return <div style={styles.header}>dishlist</div>;
};

export default Header;
