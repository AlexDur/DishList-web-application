const styles = {
  header: {
    marginTop: 50,
    fontSize: 30,
    marginRight: -1600,
    color: "blue",
  },
};

const Header = () => {
  return <div style={styles.header}>dishlist</div>;
};

export default Header;
